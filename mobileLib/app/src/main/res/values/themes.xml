<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme.
         Switched from DarkActionBar to NoActionBar: every screen now has
         its own custom gradient header card, so the system ActionBar
         (which only ever showed the bare app name) is redundant and was
         removed for a cleaner, more app-like look. No code called
         supportActionBar anywhere, so this is a purely visual change. -->
    <style name="Theme.MobileLibApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/text_inverse</item>
        <!-- Secondary / accent brand color. -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent_dark</item>
        <item name="colorOnSecondary">@color/text_inverse</item>
        <!-- Status bar: dark navy background, white icons/text. -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- App-wide background. -->
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:navigationBarColor">@color/card</item>
    </style>
</resources>
